<%= simple_form_for(@populist) do |pop| %>
<%= pop.input :title, label: "New List Title" %>
<%= pop.input :public, as: :boolean, checked_value: true, unchecked_value: false %>
<%= pop.submit "Create" %>
<% end  %>
